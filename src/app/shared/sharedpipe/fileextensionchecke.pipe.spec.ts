import { FileextensioncheckePipe } from './fileextensionchecke.pipe';

describe('FileextensioncheckePipe', () => {
  it('create an instance', () => {
    const pipe = new FileextensioncheckePipe();
    expect(pipe).toBeTruthy();
  });
});
